{{#foreach navigation}}
  <li class='c-nav__item'>
    <a
      href='{{ url absolute='true' }}'
      class='main-nav-link {{#if current}} c-nav__link--current {{/if}}'
    >
    {{ label }}
  </a>
  </li>
{{/foreach}}
